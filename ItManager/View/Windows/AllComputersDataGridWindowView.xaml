<Window x:Class="ItManager.View.Windows.AllComputersDataGridWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AllComputersDataGridWindowView"
        Height="1080"
        Width="1920"
        DataContext="{Binding Source={StaticResource AllComputersViewModel}}">
    <ScrollViewer>
        <DataGrid ItemsSource="{Binding ComputerViewModels}"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Item1}" />
                <DataGridTextColumn Binding="{Binding Path=Item2.Computer.Name}" />
                <DataGridTextColumn Binding="{Binding Path=Item2.Computer.AnyDesk.Id}" />
                <DataGridTextColumn Binding="{Binding Path=Item2.Computer.AnyDesk.Password}" />
            </DataGrid.Columns>
        </DataGrid>
    </ScrollViewer>
</Window>
