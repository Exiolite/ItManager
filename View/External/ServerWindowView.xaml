<Window DataContext="{StaticResource ServerViewModel}"
        x:Class="View.External.ServerWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding PropertyServer.Name}"
        Width="400"
        ResizeMode="NoResize">
    <StackPanel Margin="16">
        <!--#region ComputerDescription -->
        <StackPanel>
            <TextBox Text="{Binding PropertyServer.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="{StaticResource MainForeground}"
                     FontSize="36"
                     BorderThickness="0"
                     Background="{StaticResource Clear}" />
            <TextBox Text="{Binding PropertyServer.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Foreground="{StaticResource MainForeground}"
                     BorderThickness="0"
                     TextWrapping="Wrap"
                     Margin="2,0,0,0"
                     Background="{StaticResource Clear}" />
        </StackPanel>
        <!--#endregion-->

        <!--#region RemoteServices -->
        <StackPanel Margin="0,16,0,0"
            Orientation="Horizontal"
                    HorizontalAlignment="Left">
            <Button Content="RDP"
                    Margin="0,0,4,0"
                    Padding="16,4,16,4"
                    BorderThickness="0"
                    Background="#5f9597"
                    Foreground="{StaticResource PanelForeground}"
                    FontSize="20" />
        </StackPanel>
        <!--#endregion-->
    </StackPanel>
</Window>