<UserControl x:Class="View.External.ExternalMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:View.External"
             mc:Ignorable="d">
    <DockPanel >
        <StackPanel>
            <StackPanel>
                <Button Content="Add Company"
                        DataContext="{Binding Source={StaticResource CompaniesTableViewModel}}"
                        Command="{Binding CommandAddCompany}" />
            </StackPanel>
            <local:CompanyStructureView DockPanel.Dock="Left" />
        </StackPanel>
    </DockPanel>
</UserControl>
