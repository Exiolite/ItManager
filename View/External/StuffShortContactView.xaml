<UserControl DataContext="{StaticResource StuffViewModel}"
             x:Class="View.External.StuffShortContactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View.External"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Text="Firstname:" />
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Text="Lastname: " />
        <TextBlock Grid.Row="2"
                   Grid.Column="0"
                   Text="Surename" />
        <TextBlock Grid.Row="3"
                   Grid.Column="0"
                   Text="Pthone number: " />
        <TextBox Grid.Row="0"
                 Grid.Column="1"
                 Text="{Binding Path=PropertyStuff.PropertyFirstName}" 
                 Style="{StaticResource MainTextBox}"/>
        <TextBox Grid.Row="1"
                 Grid.Column="1"
                 Text="{Binding Path=PropertyStuff.PropertySecondName}"
                 Style="{StaticResource MainTextBox}" />
        <TextBox Grid.Row="2"
                 Grid.Column="1"
                 Text="{Binding Path=PropertyStuff.PropertySureName}"
                 Style="{StaticResource MainTextBox}" />
        <TextBox Grid.Row="3"
                 Grid.Column="1"
                 Text="{Binding Path=PropertyStuff.PropertyPhoneNumber}"
                 Style="{StaticResource MainTextBox}" />
    </Grid>
</UserControl>
