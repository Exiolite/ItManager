<UserControl DataContext="{Binding Source={StaticResource CompanyTableViewModel}}"
    x:Class="View.External.CompanyStructureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <DockPanel>
        <TextBlock Text="Companies"
                   FontSize="36"
                   FontFamily="SegoeUi"
                   Foreground="{StaticResource fff}"
                   DockPanel.Dock="Top">
            <TextBlock.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Add Company" 
                              Command="{Binding CommandAddNew}"/>
                </ContextMenu>
            </TextBlock.ContextMenu>
        </TextBlock>
        <TreeView Name="CompanyTableView"
                  ItemsSource="{Binding Property}"
                  Background="{StaticResource Clear}"
                  BorderThickness="0">
            <TreeView.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                                 Color="Transparent" />
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}"
                                 Color="Black" />
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}"
                                 Color="Transparent" />
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}"
                                 Color="Black" />
            </TreeView.Resources>
            <TreeView.ItemTemplate>
                <DataTemplate>
                    <TreeViewItem Header="{Binding Property.Name}"
                                  FontSize="28"
                                  Foreground="{StaticResource ff}">
                        <TreeViewItem Header="Devices"
                                      FontSize="24"
                                      Foreground="{StaticResource ff}">
                            <TreeViewItem Header="Computers"
                                          FontSize="20"
                                          Foreground="{StaticResource ff}"
                                          ItemsSource="{Binding PropertyComputerTableViewModel.PropertyPersonal}">
                                <TreeViewItem.ContextMenu>
                                    <ContextMenu BorderThickness="0"
                                                 Background="{StaticResource bbb}"
                                                 Foreground="{StaticResource ff}">
                                        <MenuItem Header="Add Computer"
                                                  Command="{Binding PropertyComputerTableViewModel.CommandAddNew}" />
                                    </ContextMenu>
                                </TreeViewItem.ContextMenu>
                                <TreeViewItem.ItemTemplate>
                                    <DataTemplate>
                                        <Button Content="{Binding Property.Name}"
                                                FontSize="16"
                                                Margin="4"
                                                Background="{StaticResource Clear}"
                                                Foreground="{StaticResource fff}"
                                                BorderThickness="0"/>
                                    </DataTemplate>
                                </TreeViewItem.ItemTemplate>
                            </TreeViewItem>
                            <TreeViewItem Header="Servers"
                                          FontSize="20"
                                          Foreground="{StaticResource ff}"
                                          ItemsSource="{Binding PropertyComputerTableViewModel.Property}">
                                <TreeViewItem.ContextMenu>
                                    <ContextMenu BorderThickness="0"
                                                 Background="{StaticResource bbb}"
                                                 Foreground="{StaticResource ff}">
                                        <MenuItem Header="Add Computer"
                                                  Command="{Binding PropertyComputerTableViewModel.CommandAddNew}" />
                                    </ContextMenu>
                                </TreeViewItem.ContextMenu>
                            </TreeViewItem>
                        <TreeViewItem Header="Stuff"
                                      Foreground="{StaticResource ff}">

                        </TreeViewItem>
                    </TreeViewItem>
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </DockPanel>
</UserControl>
