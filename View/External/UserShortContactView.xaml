<UserControl DataContext="{StaticResource UserViewModel}"
             x:Class="View.External.UserShortContactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:View.External"
             mc:Ignorable="d">
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="{Binding PropUser.PropFirstName}"
                   Style="{StaticResource TH4}"/>
        <TextBlock Text="{Binding PropUser.PropSureName}"
                   Style="{StaticResource TH4}" />
        <TextBlock Text="{Binding PropUser.PropPhoneNumber}"
                   Style="{StaticResource TH4}" />
    </StackPanel>
</UserControl>
